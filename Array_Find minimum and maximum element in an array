pair<long long, long long> getMinMax(long long a[], int n) {
    int min_val = INT_MAX;
    int max_val = INT_MIN;
    for(int i = 0; i < n; i++){
        if(a[i] < min_val)
            min_val = a[i];
        if(a[i] > max_val)
            max_val = a[i];
    }
    return {min_val, max_val};
}
